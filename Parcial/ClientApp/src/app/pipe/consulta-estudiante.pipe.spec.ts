import { ConsultaEstudiantePipe } from './consulta-estudiante.pipe';

describe('ConsultaEstudiantePipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultaEstudiantePipe();
    expect(pipe).toBeTruthy();
  });
});
